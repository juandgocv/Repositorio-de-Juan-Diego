# Workshop: Support Vector Machine Classification

**Tools:** Python · scikit-learn · Pandas  
**Topic:** Supervised Learning · Kernel Methods · SVM

SVM classifier with comparison of linear and RBF kernels. Includes hyperparameter tuning via grid search and evaluation with classification metrics.

> **Note:** The large training datasets (up to 300k rows) are not included in the repository due to size constraints. The notebook is self-documented.
